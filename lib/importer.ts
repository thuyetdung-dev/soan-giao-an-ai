export type SourceDoc={name:string;size:number;text:string;kind:string};
export async function readSource(file:File):Promise<SourceDoc>{const ext=file.name.split(".").pop()?.toLowerCase()||"";let text="";if(["txt","md","json"].includes(ext))text=await file.text();else if(ext==="docx"){const mammoth=await import("mammoth/mammoth.browser");text=(await mammoth.extractRawText({arrayBuffer:await file.arrayBuffer()})).value}else if(ext==="pdf"){const pdfjs=await import("pdfjs-dist/legacy/build/pdf.mjs");pdfjs.GlobalWorkerOptions.workerSrc=`https://cdn.jsdelivr.net/npm/pdfjs-dist@5.4.149/legacy/build/pdf.worker.min.mjs`;const pdf=await pdfjs.getDocument({data:new Uint8Array(await file.arrayBuffer())}).promise;const pages=[];for(let i=1;i<=Math.min(pdf.numPages,80);i++){const page=await pdf.getPage(i),content=await page.getTextContent();pages.push(content.items.map(item=>"str" in item?item.str:"").join(" "))}text=pages.join("\n")}else throw new Error(`Chưa hỗ trợ tệp .${ext}`);return{name:file.name,size:file.size,text:text.slice(0,120000),kind:ext}}
