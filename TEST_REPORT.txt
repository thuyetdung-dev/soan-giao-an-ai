LESSONSTUDIO V8.4 — TEST REPORT

Ngày kiểm tra: 2026-09-08
Kết quả: 35/35 PASSED

Bổ sung 6 kiểm thử Visual Recovery & Teacher Asset Hub:
- Chỉ nhận diện biểu thức hàm được viết rõ ràng.
- Tự khôi phục đồ thị từ biểu thức đã kiểm tra.
- Tự dựng đúng bảng biến thiên của hàm bậc ba kiểm chứng được.
- Yêu cầu giáo viên cung cấp hình khi thiếu dữ liệu.
- Ảnh hợp lệ do giáo viên tải lên thỏa Visual Contract.
- Visual tự dựng chỉ được mở khóa sau khi giáo viên duyệt.

Bổ sung 4 kiểm thử Math Visual First:
- Phát hiện slide tham chiếu đồ thị nhưng không có graph/image.
- Phát hiện slide nhắc bảng biến thiên nhưng không có variation_table.
- Chấp nhận slide visual có graph hợp lệ về cấu trúc.
- Tự tách slide vượt ngân sách mật độ trước khi render.

PASSED — Hai đề khác nhau tạo fingerprint khác nhau.
PASSED — answer_index và check.correct_index đồng bộ sau khi đảo phương án.
PASSED — Đáp án chữ cái và cụm "chọn A/B/C/D" trong lời giải được đồng bộ.
PASSED — Parser chấp nhận biểu thức toán học hợp lệ.
PASSED — Parser từ chối truy cập thuộc tính.
PASSED — Parser từ chối hàm ngoài danh sách cho phép.
PASSED — Bảng biến thiên đúng được chấp nhận.
PASSED — Bảng sai dấu hoặc bỏ sót điểm tới hạn bị từ chối.
PASSED — Tự sửa an toàn ẩn bảng không kiểm chứng được, giữ nội dung và chuyển trạng thái sang REVIEW.
PASSED — Hồ sơ và storyboard đầy đủ, đúng 45 phút, qua cổng CTGDPT 2018.
PASSED — Sai ngân sách thời gian bị chặn.
PASSED — Yêu cầu cần đạt thiếu minh chứng bị chặn.
PASSED — Storyboard tham chiếu slide không tồn tại bị chặn.
PASSED — Nhận diện đúng lỗi 429/quota.
PASSED — Tự chuyển từ mô hình hết hạn mức ngày sang mô hình dự phòng.
PASSED — Luôn ưu tiên mô hình do giáo viên lựa chọn trước các mô hình dự phòng.
PASSED — Phát hiện đồng thời bài thiếu nhiều slide và thiếu pha hoạt động.
PASSED — Chấp nhận sai lệch tối đa 2 slide khi đã đủ 5 pha.
PASSED — Chỉ ưu tiên bản sửa có ít lỗi FAIL hơn, kể cả khi số slide lệch nhẹ.
PASSED — Chia đúng các phạm vi 10 slide và xử lý đúng chặng cuối không đủ 10.
PASSED — Nhật ký liên tục lưu được kiến thức và công thức của slide trước.
PASSED — Phát hiện và từ chối slide trùng lặp.
PASSED — Chấp nhận slide mới có nội dung khác biệt.
PASSED — Bản đồ toàn bài phải đủ số thứ tự liên tục và không trùng tiêu đề.
PASSED — Chấp nhận nhiều tệp nguồn trong giới hạn và từ chối khi vượt số lượng cho phép.

Lệnh tái kiểm tra:
python -m unittest discover -s tests -v
